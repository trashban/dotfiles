{
	"layer": "top",
	"position": "top",

    "modules-left": ["sway/workspaces", "sway/window", "mpris", "tray"],
    "modules-right": ["network", "temperature", "disk", "memory", "wireplumber", "clock"],

    // hyprland

	"hyprland/workspaces": {
        "format": "{}",
        "sort-by-coordinates": true,
        "all-output": false,
        "active-only": false,
        "sort-by-number": false,
        "on-click": "activate"
    },

	"hyprland/window": {
    	"format": "{initialTitle}",
    	"max-length": 30
    },

    // niri

	"niri/window": {
    	"format": "{title}",
    	"max-length": 30
    },

    // river

    "river/tags": {
        "hide-vacant": true,
    },

    "river/window": {
        "max-length": 40,
    },

    // sway

    "sway/workspaces": {
        "disable-scroll": true,
        "all-outputs": true,
        "format": "{icon}",
        "format-icons": {
            "1": "I",
            "2": "II",
            "3": "III",
            "4": "IV",
            "5": "V",
            "6": "VI",
            "7": "VII",
            "8": "VIII",
            "9": "IX",
            "10": "X"
        }
    },

    "sway/window": {
        "format": "{title}",
        "max-length": 30,
        "rewrite": {
            "": "sway",
        }
    },

    "mpris": {
        "format": "{status_icon} {title}",
        "format-paused": "{status_icon} {title}",
        "status-icons": {
            "playing": "▶",
            "paused": "⏸"
        },
        // "ignored-players": ["firefox", "firefox-bin"]
    },

    "tray": {
        "icon-size": 18,
        "spacing": 15
    },

    "network": {
        "format-wifi": "{ipaddr}",
        "format-disconnected": "睊",
        "interval": 10
    },

    "temperature": {
        "hwmon-path": "/sys/devices/pci0000:00/0000:00:18.3/hwmon/hwmon3/temp1_input",
		"format": "| {temperatureC}°C",
		"format-icons":[""],
		"interval": 30
    },

    "disk": {
        "format": "| {percentage_used}%",
        "interval": 60
    },

    "memory": {
        "format": "| {used:0.1f}G",
        "interval": 5
    },

    "cpu": {
        "format": "<span color='#b2ccd6'>CPU</span>: {avg_frequency} GHz",
        "interval": 5
    },

    "wireplumber": {
        "format": "| {volume}%",
        "format-muted": "MUTED"
    },

    "clock": {
        // %a %b %e,
        "format": "| {:%I:%M %p}",
        "interval": 30
    }
}
